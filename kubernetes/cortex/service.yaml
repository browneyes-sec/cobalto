apiVersion: v1
kind: Service
metadata:
  name: cortex
  namespace: cortex
  labels:
    app.kubernetes.io/name: cortex
spec:
  type: ClusterIP
  ports:
    - port: 9001
      targetPort: 9001
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: cortex
