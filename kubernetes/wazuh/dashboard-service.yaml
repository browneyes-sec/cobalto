apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
  namespace: wazuh
  labels:
    app.kubernetes.io/name: wazuh-dashboard
    app.kubernetes.io/part-of: wazuh
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: wazuh-dashboard
  ports:
    - name: https
      port: 443
      targetPort: 443
      protocol: TCP
